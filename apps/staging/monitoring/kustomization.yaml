apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - uptime-kuma.yaml
  - networkpolicy.yaml
  # cloudflared and its Secret are an atomic pair (enabled together, README
  # "enable the tunnel" step) — removing either one alone leaves the apps
  # Kustomization (wait: true) red on a CreateContainerConfigError pod.
  - cloudflared.yaml
  - cloudflared-token.sops.yaml
